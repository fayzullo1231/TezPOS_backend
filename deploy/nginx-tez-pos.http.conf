# TezPOS — tez-pos.uz (HTTP; certbot HTTPS qo'shishi mumkin)
# Media/static: /opt/tezpos-backend

server {
    listen 80;
    listen [::]:80;
    server_name tez-pos.uz www.tez-pos.uz;

    client_max_body_size 50M;

    gzip on;
    gzip_comp_level 5;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types application/json application/javascript text/plain text/css text/xml application/xml;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location /media/ {
        alias /opt/tezpos-backend/media/;
        access_log off;
        expires 7d;
    }

    location /static/ {
        alias /opt/tezpos-backend/staticfiles/;
        access_log off;
        expires 7d;
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}
